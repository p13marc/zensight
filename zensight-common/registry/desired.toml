# Subject registry: @desired (fleet desired-state author). RFC 07 §3, RFC 12 §3;
# issue #816.
#
# A controller publishes runtime POLICY for a target host under this service
# origin, with the TARGET HOST ID AS THE FIRST SUBJECT CHUNK (the G1
# desired-state proxy rule; zenkey-build's H4 lint enforces the ordering).
# LWW + storage-backed: the target reconciles on connect and reconnect, so a
# sensor that was offline during a change picks it up when it returns. This is
# convergence, never commands — RFC 12 is explicit that durable pub/sub
# imperatives are the permanently forbidden alternative.
#
# What must NEVER move onto this origin (the single most important constraint
# in #816): secrets, and anything a sensor needs to REACH THE BUS — endpoints,
# TLS material, the namespace. A sensor must always find its router from local
# file config, or one bad desired publish locks the fleet out of its own
# supervision. Structurally enforced on the consumer side: the reconciler
# (zensight-sensor-core::desired) only ever deserializes a sentinel's own
# config type and only ever writes that sentinel's handle.
#
# ttl_s = 0 on every subject: desired state is operator policy — it never
# ages out; it is replaced or deleted (a delete reverts the target to its
# file-config baseline).

[registry]
version = "1.4"
app = "zensight"
convention = 1

[service]
name = "desired"
origin = "@desired"
description = "fleet desired-state author (#816): per-host runtime policy, target host first subject chunk; LWW, storage-backed, reconciled on (re)connect"

# Topics join as their config types migrate to zensight-common (#849): a
# state-class subject needs a real schemars-generated schema, and a
# sensor-crate type can never provide one — `zensight-common` cannot depend on
# a sensor, so `describe` could only carry a summary stub. #815's gate refused
# exactly that, correctly.
#
# hostspec (#816), systemd, netlink and logs (#849) have all migrated their
# sentinel vocabularies to `zensight-common`. That was the whole of #849, and
# with it every sentinel in the tree is fleet-authorable.
#
# netlink's set is registered as `NetlinkExpectations` rather than
# `ExpectationsConfig`: the type table is flat and systemd's set holds that
# name. Nothing breaks — netlink's shipped `@rpc/netlink/expectations/set`
# declares `ExpectationCommand`, a tagged enum of incremental operations that
# is a genuinely different shape, and keeps it. `LogRulesConfig` keeps its
# name; it only stops being a summary stub and becomes a real schema, which is
# what #815's gate requires before a type may carry a state-class subject.
#
# `thresholds` (#931) needed no migration: `ThresholdsConfig` was written in
# `zensight-common` from the start (#928) and is schema-registered, so every
# producer joins at once rather than one per sentinel rewrite. One rule
# vocabulary, authored fleet-wide, evaluated at the edge by whichever sensor
# publishes the metric.

[[subject]]
path = "{host}/hostspec/expectations"
class = "state"
type = "HostspecExpectations"
cardinality = 10000
ttl_s = 0
since = "1.0"
description = "desired hostspec assertion set for {host} (#821 — its natural fleet-wide author)"

# The type name is `ExpectationsConfig` rather than a producer-prefixed
# `SystemdExpectations`, unlike hostspec's above, for one reason: that is the
# name `@rpc/systemd/expectations/set` has advertised since 1.0, nothing
# collides with it (netlink's set takes `ExpectationCommand`, logs' takes
# `LogRulesConfig`), and renaming it would be a breaking registry change to a
# shipped path for a payload whose bytes do not change.
[[subject]]
path = "{host}/systemd/expectations"
class = "state"
type = "ExpectationsConfig"
cardinality = 10000
ttl_s = 0
since = "1.1"
description = "desired systemd unit-expectation set for {host} (#277 — services/targets active, timer freshness and success, restart rates, forbid-failed)"

# netlink's expectation set (#113, #323, #849). The type name is
# `NetlinkExpectations` because the flat type table already holds
# `ExpectationsConfig` for systemd's; the wire payload is netlink's own plain
# set, the same one `expectations/set` applies after unwrapping its command
# envelope.
[[subject]]
path = "{host}/netlink/expectations"
class = "state"
type = "NetlinkExpectations"
cardinality = 10000
ttl_s = 0
since = "1.4"
description = "desired netlink expectation set for {host} (#113, #323 — sockets, links, neighbours, routes, rates, delivery floors, route flaps, policy rules)"

# logs' sentinel ruleset (#543, #849). Same type name `@rpc/logs/rules/set` has
# advertised since 2.2 — the bytes do not change, only the schema behind the
# name becomes real.
[[subject]]
path = "{host}/logs/rules"
class = "state"
type = "LogRulesConfig"
cardinality = 10000
ttl_s = 0
since = "1.4"
description = "desired log sentinel ruleset for {host} (#543 — pattern→alert rules with windowed thresholds and rate limits)"

[[subject]]
path = "{host}/sysinfo/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.2"
description = "desired threshold rule set for sysinfo on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/logs/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.2"
description = "desired threshold rule set for logs on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/systemd/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.2"
description = "desired threshold rule set for systemd on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/hostspec/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.2"
description = "desired threshold rule set for hostspec on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/probe/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.2"
description = "desired threshold rule set for probe on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/snmp/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for snmp on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/netlink/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for netlink on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/netring/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for netring on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/container/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for container on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/pve/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for pve on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/bmc/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for bmc on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/parallax/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for parallax on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/gnmi/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for gnmi on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/modbus/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for modbus on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

[[subject]]
path = "{host}/netflow/thresholds"
class = "state"
type = "ThresholdsConfig"
cardinality = 10000
ttl_s = 0
since = "1.3"
description = "desired threshold rule set for netflow on {host} (#931): rules over the metrics this producer publishes, evaluated on its own publish path"

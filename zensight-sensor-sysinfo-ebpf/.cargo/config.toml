# Default to the BPF target + build-std so a manual `cargo build` in this crate
# works the same way `aya-build` invokes it from the parent build.rs.
[build]
target = "bpfel-unknown-none"

[unstable]
build-std = ["core"]
